// Operadores de mayor y menor ... < > que

const dinero = 500;
const totalPagar = 300;

if(dinero > totalPagar) {
    console.log('Se puede pogar');
} else {
    console.log('Fondos insuficientes');
}