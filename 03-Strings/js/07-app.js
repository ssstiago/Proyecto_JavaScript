const producto = 'Monitor 20 pulgadas';
console.log(producto.toUpperCase()); // Transforma strings en MAYUSCULAS
console.log(producto.toLowerCase()); // Transforma strings en minusculas

//Convertir numeros a strings ()uso de metodos
const precio = 300;
console.log(precio);
console.log(precio.toString());